package ru.rentoptima.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;
import ru.rentoptima.entity.FeedbackAnswer;
import ru.rentoptima.entity.FeedbackQuestion;
import ru.rentoptima.entity.FeedbackResponse;

import java.time.LocalDateTime;
import java.util.List;

// FeedbackQuestionRepository
interface FeedbackQuestionRepositoryTemp { }

// paste each into its own file — see below

